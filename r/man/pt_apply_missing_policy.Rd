% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage.R
\name{pt_apply_missing_policy}
\alias{pt_apply_missing_policy}
\title{Drop under-observed features, then resolve the remaining missing values}
\usage{
pt_apply_missing_policy(
  data,
  policy = "as_is",
  min_observed_fraction = 0,
  verbose = TRUE
)
}
\arguments{
\item{data}{a numeric matrix, features x samples.}

\item{policy}{one of \code{"as_is"}, \code{"zero"}, \code{"low"}.}

\item{min_observed_fraction}{minimum fraction of samples a feature must be
measured in.}

\item{verbose}{print/warn about what was done.}
}
\value{
The resolved matrix.
}
\description{
\code{min_observed_fraction} drops any feature measured in fewer than that
fraction of samples (0, the default, drops nothing beyond all-missing rows).
\code{policy} then decides the rest:
}
\details{
\itemize{
\item \code{as_is} leave them for the backend, warning that something else will decide.
The default -- it changes no existing result.
\item \code{zero} fill with 0 explicitly, making the de-facto behaviour a stated choice.
\item \code{low} fill with one unit below each sample's own observed minimum, so an
unmeasured feature ranks below every measured one.
}
}
