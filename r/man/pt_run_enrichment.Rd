% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{pt_run_enrichment}
\alias{pt_run_enrichment}
\title{Run (or load from cache) the configured enrichment backend}
\usage{
pt_run_enrichment(fm, config, verbose = TRUE)
}
\arguments{
\item{fm}{a \code{\link[=pt_feature_matrix]{pt_feature_matrix()}}.}

\item{config}{an enrichment section, from \code{\link[=pt_enrichment_config]{pt_enrichment_config()}}.}

\item{verbose}{print progress and coverage warnings.}
}
\value{
A \code{\link[=pt_score_matrix]{pt_score_matrix()}}.
}
\description{
The cache is keyed on backend + geneset only, so two configs that differ in
which samples they select share one file.  A requested sample that is not in
the cache is therefore scored and added, never silently dropped: with a
per-sample backend a sample's scores do not depend on which other samples
were run, so filling the cache in is exact.
}
