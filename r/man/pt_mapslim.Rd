% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obo.R
\name{pt_mapslim}
\alias{pt_mapslim}
\title{Map a GO term onto its GO-slim ancestors}
\usage{
pt_mapslim(go_id, obo, slim, cache = new.env(parent = emptyenv()))
}
\arguments{
\item{go_id}{a GO id.}

\item{obo}{the full ontology, from \code{\link[=pt_read_obo]{pt_read_obo()}}.}

\item{slim}{the GO-slim ontology, from \code{\link[=pt_read_obo]{pt_read_obo()}}.}

\item{cache}{an environment used to memoise ancestor sets across calls.}
}
\value{
A list with \code{direct} and \code{all} character vectors of slim ids.
}
\description{
Reproduces \code{goatools.mapslim}: \code{all} is every slim term on a path from the
term to the root (the term itself included), and \code{direct} is the subset that
no other member of \code{all} sits below -- the most specific slim ancestors.
}
