% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categories.R
\name{pt_summarize_by_category}
\alias{pt_summarize_by_category}
\title{Aggregate a result table by broad category}
\usage{
pt_summarize_by_category(
  table,
  mapping,
  key_col = "pathway",
  value_col = "effect",
  group_cols = NULL,
  stat = "mean",
  unmapped_label = "Other",
  significance_col = NULL,
  alpha = 0.05
)
}
\arguments{
\item{table}{a long data.frame with a pathway column.}

\item{mapping}{a named term -> category vector, or a list.}

\item{key_col}{the pathway column in \code{table}.}

\item{value_col}{the numeric column to aggregate.}

\item{group_cols}{extra columns to aggregate within, e.g. \code{"comparison"}.}

\item{stat}{\code{"mean"}, \code{"median"} or \code{"sum"}.}

\item{unmapped_label}{category assigned to terms absent from \code{mapping}.}

\item{significance_col}{optional column to split on.}

\item{alpha}{significance threshold.}
}
\value{
A data.frame sorted by the grouping keys.
}
\description{
Returns one row per (group..., [significance,] category) with the
aggregated value, \code{n_pathways}, \code{n_up} and \code{n_down}.
}
\details{
If \code{significance_col} is given (e.g. \code{"fdr"}), each category is split into
\code{significant} (value \verb{< alpha}) vs \code{non_significant} rows, adding a
\code{significance} column.  \code{NA} thresholds count as non-significant.
}
